function addingTo(input){

    
    return aForLoop(input);
}

function aForLoop(input){

    //a for loop
    for (var i = 0; i < 1;i++ ){
        input -= i;
    }

}

//Adding two to the number in the method
addingTo(2);